# Claude Legacy Archive

This directory preserves selected Claude-era documents for historical reference only.

These files are no longer active sources of truth for this repository.

Use the following active locations instead:

- Repo instructions: `AGENTS.md`
- Reusable skills: `.agents/skills/`

Archived items here exist only to preserve historical context after retiring the old `.claude` workflow.
