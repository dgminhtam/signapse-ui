interface:
  display_name: "API Mapping Sync"
  short_description: "Sync api_mapping.json into APIMAPPING.md"
  default_prompt: "Use $api-mapping-sync to compare docs/api_mapping.json with docs/APIMAPPING.md, update the frontend integration notes, and summarize FE impact."
