interface:
  display_name: "API Mapping Sync"
  short_description: "Update APIMAPPING.md and report API diffs"
  default_prompt: "Use $api-mapping-sync to sync docs/APIMAPPING.md with the live dev OpenAPI contract, report confirmed API diffs, and summarize frontend impact."
